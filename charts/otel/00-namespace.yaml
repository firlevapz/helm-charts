apiVersion: v1
kind: Namespace
metadata:
  name: observability
  labels:
    # The agent DaemonSet mounts /var/log/pods (hostPath) to tail container logs.
    # baseline policy forbids hostPath volumes, so this namespace runs privileged.
    pod-security.kubernetes.io/enforce: privileged

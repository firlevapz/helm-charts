{{- if .Values.databases.office.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "grommunio.officeDb.fullname" . }}
  labels:
    {{- include "grommunio.officeDb.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
      protocol: TCP
  selector:
    {{- include "grommunio.officeDb.selectorLabels" . | nindent 4 }}
{{- end }}

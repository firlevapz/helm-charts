{{- if .Values.databases.gromox.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "grommunio.gromoxDb.fullname" . }}
  labels:
    {{- include "grommunio.gromoxDb.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
      protocol: TCP
  selector:
    {{- include "grommunio.gromoxDb.selectorLabels" . | nindent 4 }}
{{- end }}

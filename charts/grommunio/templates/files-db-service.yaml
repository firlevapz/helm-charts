{{- if .Values.databases.files.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "grommunio.filesDb.fullname" . }}
  labels:
    {{- include "grommunio.filesDb.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: mysql
      port: 3306
      targetPort: mysql
      protocol: TCP
  selector:
    {{- include "grommunio.filesDb.selectorLabels" . | nindent 4 }}
{{- end }}
